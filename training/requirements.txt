# Install torch + PyG matching your CUDA (A100 → cu121 wheels), e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install torch_geometric
torch>=2.1
torch_geometric>=2.4        # DeepGraphInfomax, GDC, dropout_edge, coalesce
scikit-learn>=1.3           # KMeans + NMI for eval
pandas>=2.0
numpy>=1.24
sentence-transformers>=2.2  # gab only (text-embedding node features)
